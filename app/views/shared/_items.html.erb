<%= render partial: 'shared/category_sidebar', locals: { categories: @categories} %>
<div class='align-to-sidebar'>
<% categories.each do |category| %>
<span id="<%= category.name %>">
  <div class="col-md-10">
  </br></br>
  <table class="table">
    <h2><%= category.name %></h2>
    <thead>
      <tr>
        <th></th>
        <th>Item</th>
        <th>Unit Size</th>
        <th>Units Available</th>
        <th>Unit Price</th>
        <th></th><th></th>
      </tr>
    </thead>
    <tbody>
      <% category.items.each do |item| %>
      <div>
        <tr>
          <td><%= image_tag item.picture.url(:thumb), :size => '32x32', :class => "img-rounded" %></td>
          <td class='col-md-3'><%= link_to item.title, item_path(item) %></td>
          <td ><%= item.size %></td>
          <td><%= item.inventory %></td>
          <td><%= item.price %></td
          <td></td><td class='col-md-3'><a class='details-link'>More Details</a></td>
          <td><button class='btn btn-primary cart-link'>Add to Cart</button></td>
        </tr>
        <tr class='details-info hidden'>
          <td></td>
          <td class='col-md-3'>Description: <br/><%= item.description %></td>
          <td></td><td></td><td></td>
          <td class='col-md-3'>Supplier: <br/><%= item.supplier.name %></td>

        </tr>
        <tr class='cart-info hidden'>
          <td></td></td><td></td><td></td><td></td><td></td>
          <td>
            <%= form_tag(add_to_cart_path) do %>
            <input type="hidden" name="item_id" value="<%= item.id %>">
            <div class="col-md-10">
              <div class="input-group">
                <span class="input-group-addon">Qty</span>
                <input type="text" class="form-control" name="quantity" value="1">
              </div>
            </div>
          </td>
          <td>
            <div class="col-md-3">
              <%= button_tag 'Submit', class: 'btn btn-danger' %>
            </div>
            <% end %>
        </tr>
      </div>
      <% end %>
    </tbody>
  </table>
  </div>
</span>
<% end %>
</div
<script src="//code.jquery.com/jquery-1.11.0.min.js"></script>
<script src="assets/javascripts/application.js" type="text/javascript" charset="utf-8"></script>

<!-- <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
<script src="application.js"></script> -->

<!-- <div class="col-md-10">
<% categories.each do |category| %>
  <div class="row">
    <div class="col-md-12">
      <h1><%= category.name %></h1>
    </div>
  </div>
  <% category.items.each_slice(4) do |slice| %>
    <div class="row">
      <% slice.each do |item| %>
        <div class="col-md-3">
          <div class="thumbnail">
            <%= image_tag item.picture.url(:medium), :class => "img-rounded" %>
            <div class="caption">
              <h3><%= link_to item.title, item_path(item) %></h3>
              <p><%= item.description %></p>
              <p><%= item.price %></p>
              <div class="row">
                <%= form_tag(add_to_cart_path) do %>
                  <input type="hidden" name="item_id" value="<%= item.id %>">
                  <div class="col-md-6">
                    <div class="input-group">
                      <span class="input-group-addon">Qty</span>
                      <input type="text" class="form-control" name="quantity" value="1">
                    </div>
                  </div>
                  <div class="col-md-6">
                    <%= button_tag 'Add To Cart', class: 'btn btn-primary' %>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
<% end %>
</div> -->
