<h3>Order Items:</h3>

  <table class="table">
    <thead>
      <tr>
        <th>Item:</th>
        <th>Price:</th>
        <th>Quantity:</th>
        <th>Subtotal:</th>
      </tr>
    </thead>

    <tbody>

        <% @order_items.each do |order_item| %>
          <% @order_item = order_item %>
          <%= form_for([:dashboard, current_supplier, @sub_order, @order_item], :html => { :class => "form-horizontal"}) do |f| %>

          <%= render partial: 'shared/error_messages', locals: { obj: order_item } %>

        <tr>
          <td><strong><%= order_item.item.title %></strong></td>
          <td><%= f.text_field :price, class: "form_control edit-order-item-box", value: order_item.edit_price %></td>
          <td><%= f.text_field :quantity, class: "form-control edit-order-item-box" %></td>
          <td><%= order_item.subtotal %></td>
          <td><%= f.submit value="Save Changes", class: "btn update-order-item-btn" %></td>
        </tr>

        <% end %>
        <% end %>
      </tr>
    </tbody>
  </table>
  <%= link_to 'Back to Edit Order', edit_dashboard_supplier_sub_order_path(current_supplier, @sub_order) %>
